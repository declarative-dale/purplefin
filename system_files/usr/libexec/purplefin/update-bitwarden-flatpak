#!/usr/bin/env bash
set -euo pipefail

app_id="com.bitwarden.desktop"

if ! flatpak info --system "${app_id}" >/dev/null 2>&1; then
	echo "${app_id} is not installed system-wide; nothing to update"
	exit 0
fi

flatpak update --system --assumeyes --noninteractive "${app_id}"

name: Build one Purplefin profile

on:
  workflow_call:
    inputs:
      upstream-base-digest:
        required: true
        type: string
      build-input:
        required: true
        type: string
      profile:
        required: true
        type: string
      parent-digest:
        required: false
        default: ""
        type: string
      parent-image:
        required: true
        type: string
      parent-from-lock:
        required: false
        default: false
        type: boolean
      parent-profile:
        required: false
        default: ""
        type: string
      parent-tag:
        required: false
        default: ""
        type: string
      reuse-existing:
        required: false
        default: true
        type: boolean
      upstream-source:
        required: false
        default: bluefin
        type: string
      version:
        required: true
        type: string

env:
  IMAGE_REF: ghcr.io/${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Initialize build report
        shell: bash
        run: echo "BUILD_STARTED_AT=$(date +%s)" >>"${GITHUB_ENV}"

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Nix and Cachix
        uses: ./.github/actions/setup-nix

      - name: Log in to GHCR for staging and build cache
        env:
          GHCR_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          install -d -m 0700 "${HOME}/.docker"
          registry_auth_file="${HOME}/.docker/config.json"
          printf '%s' "${GHCR_TOKEN}" |
            podman login \
              --authfile "${registry_auth_file}" \
              ghcr.io \
              --username "${GITHUB_ACTOR}" \
              --password-stdin
          echo "REGISTRY_AUTH_FILE=${registry_auth_file}" >>"${GITHUB_ENV}"

      - name: Resolve immutable build inputs
        id: inputs
        env:
          PARENT_DIGEST: ${{ inputs.parent-digest }}
          PARENT_IMAGE: ${{ inputs.parent-image }}
          PARENT_PROFILE: ${{ inputs.parent-profile }}
          PARENT_TAG: ${{ inputs.parent-tag }}
        shell: bash
        run: |
          if [[ -n "${PARENT_PROFILE}" && -z "${PARENT_DIGEST}" ]]; then
            [[ -n "${PARENT_TAG}" ]] || {
              echo 'A parent digest or tag is required for derived builds' >&2
              exit 2
            }
            PARENT_DIGEST="$(
              nix shell --accept-flake-config .#ci-skopeo \
                -c skopeo inspect --retry-times 3 --format '{{.Digest}}' \
                  "docker://${PARENT_IMAGE}:${PARENT_TAG}"
            )"
          fi
          [[ "${PARENT_DIGEST}" =~ ^sha256:[0-9a-f]{64}$ ]] || {
            echo "Invalid parent digest: ${PARENT_DIGEST}" >&2
            exit 2
          }
          {
            echo "parent_digest=${PARENT_DIGEST}"
          } >>"${GITHUB_OUTPUT}"

      - name: Reuse matching staged image
        if: inputs.reuse-existing
        id: reuse
        env:
          BUILD_INPUT: ${{ inputs.build-input }}
          BUILD_PROFILE: ${{ inputs.profile }}
          COSIGN_IDENTITY: https://github.com/${{ github.repository }}/.github/workflows/build-profile.yml@refs/heads/main
          EXPECTED_PARENT_DIGEST: ${{ steps.inputs.outputs.parent_digest }}
          EXPECTED_REVISION: ${{ github.sha }}
          EXPECTED_UPSTREAM_DIGEST: ${{ inputs.upstream-base-digest }}
          EXPECTED_VERSION: ${{ inputs.version }}
          CANDIDATE_TAG: ${{ format('{0}-candidate', inputs.profile) }}
        shell: bash
        run: |
          started=${SECONDS}
          record_duration() {
            echo "seconds=$((SECONDS - started))" >>"${GITHUB_OUTPUT}"
          }
          trap record_duration EXIT

          if digest="$(nix shell --accept-flake-config .#ci-image-reuse \
            -c purplefin-image-reuse "${CANDIDATE_TAG}")" &&
            [[ -n "${digest}" ]]; then
            if image_id="$({
              podman pull --quiet \
                --authfile "${REGISTRY_AUTH_FILE}" \
                "${IMAGE_REF}@${digest}"
            })"; then
              podman tag "${image_id}" "${IMAGE_REF}:${CANDIDATE_TAG}"
              {
                echo 'hit=true'
                echo "digest=${digest}"
              } >>"${GITHUB_OUTPUT}"
              exit 0
            fi
            echo "${BUILD_PROFILE}: matching image could not be pulled; building it again" >&2
          fi
          echo 'hit=false' >>"${GITHUB_OUTPUT}"

      - name: Load locked upstream image
        if: inputs.parent-from-lock && steps.reuse.outputs.hit != 'true'
        id: upstream_load
        env:
          UPSTREAM_SOURCE: ${{ inputs.upstream-source }}
        shell: bash
        run: |
          started=${SECONDS}
          record_duration() {
            echo "seconds=$((SECONDS - started))" >>"${GITHUB_OUTPUT}"
          }
          trap record_duration EXIT
          nix shell --accept-flake-config .#ci-load-bluefin \
            -c purplefin-load-bluefin "${UPSTREAM_SOURCE}"

      - name: Build image from immutable parent
        if: steps.reuse.outputs.hit != 'true'
        id: build
        env:
          BUILD_INPUT: ${{ inputs.build-input }}
          BUILD_PROFILE: ${{ inputs.profile }}
          PARENT_DIGEST: ${{ steps.inputs.outputs.parent_digest }}
          PARENT_FROM_LOCK: ${{ inputs.parent-from-lock }}
          PARENT_IMAGE: ${{ inputs.parent-image }}
          PARENT_PROFILE: ${{ inputs.parent-profile }}
          PARENT_TAG: ${{ inputs.parent-tag }}
          CACHE_WRITE: "true"
          PURPLEFIN_VERSION: ${{ inputs.version }}
          UPSTREAM_BASE_DIGEST: ${{ inputs.upstream-base-digest }}
        shell: bash
        run: |
          started=${SECONDS}
          record_duration() {
            echo "seconds=$((SECONDS - started))" >>"${GITHUB_OUTPUT}"
          }
          trap record_duration EXIT

          primary_image="${IMAGE_REF}:${BUILD_PROFILE}-candidate"
          created="$(date -u +'%Y-%m-%dT%H:%M:%SZ')"
          containerfile=./bootc/Containerfile
          cache_ref="${IMAGE_REF}-build-cache"
          cache_args=(--layers)
          parent_args=()

          if [[ -n "${PARENT_PROFILE}" ]]; then
            containerfile=./bootc/Containerfile.derived
            parent_args+=(
              --label "io.purplefin.parent.digest=${PARENT_DIGEST}"
              --label "io.purplefin.parent.profile=${PARENT_PROFILE}"
              --build-arg "PARENT_PROFILE=${PARENT_PROFILE}"
            )
          fi
          base_ref="${PARENT_IMAGE}@${PARENT_DIGEST}"
          pull_policy=always
          if [[ "${PARENT_FROM_LOCK}" == true ]]; then
            [[ -n "${PARENT_TAG}" ]]
            base_ref="${PARENT_IMAGE}:${PARENT_TAG}"
            pull_policy=never
          fi
          cache_available=false
          if nix shell --accept-flake-config .#ci-skopeo \
            -c skopeo list-tags "docker://${cache_ref}" >/dev/null 2>&1; then
            cache_available=true
            cache_args+=(--cache-from "${cache_ref}" --cache-ttl 168h)
          fi
          if [[ "${CACHE_WRITE}" == true ]]; then
            cache_args+=(--cache-to "${cache_ref}")
          fi
          {
            echo "cache_available=${cache_available}"
            echo "cache_ref=${cache_ref}"
            echo "cache_write=${CACHE_WRITE}"
          } >>"${GITHUB_OUTPUT}"

          buildah bud \
            --file "${containerfile}" \
            --pull="${pull_policy}" \
            --build-context purplefin-generated="$(nix build .#generated --no-link --print-out-paths)" \
            "${cache_args[@]}" \
            "${parent_args[@]}" \
            --label "io.purplefin.build.input=${BUILD_INPUT}" \
            --label "io.purplefin.build.profile=${BUILD_PROFILE}" \
            --label "io.purplefin.upstream.digest=${UPSTREAM_BASE_DIGEST}" \
            --label "org.opencontainers.image.base.digest=${PARENT_DIGEST}" \
            --label "org.opencontainers.image.created=${created}" \
            --label "org.opencontainers.image.revision=${GITHUB_SHA}" \
            --build-arg "BASE_REF=${base_ref}" \
            --build-arg "BUILD_PROFILE=${BUILD_PROFILE}" \
            --build-arg "PURPLEFIN_VERSION=${PURPLEFIN_VERSION}" \
            --format docker \
            --tls-verify=true \
            --tag "${primary_image}" \
            .

      - name: Resolve verified previous profile build
        if: steps.reuse.outputs.hit != 'true'
        id: previous_build
        env:
          BUILD_PROFILE: ${{ inputs.profile }}
          COSIGN_IDENTITY: https://github.com/${{ github.repository }}/.github/workflows/build-profile.yml@refs/heads/main
        shell: bash
        run: |
          set -euo pipefail
          mutable_ref="${IMAGE_REF}:${BUILD_PROFILE}"
          metadata="$({
            nix shell --accept-flake-config .#ci-skopeo \
              -c skopeo inspect --retry-times 3 "docker://${mutable_ref}" 2>/dev/null || true
          })"
          digest="$(jq -r '.Digest // empty' <<<"${metadata}")"
          if [[ "${digest}" =~ ^sha256:[0-9a-f]{64}$ ]] &&
            nix shell --accept-flake-config .#ci-cosign \
              -c cosign verify \
                --certificate-oidc-issuer https://token.actions.githubusercontent.com \
                --certificate-identity "${COSIGN_IDENTITY}" \
                "${IMAGE_REF}@${digest}" >/dev/null 2>&1; then
            {
              echo 'available=true'
              echo "digest=${digest}"
              echo "reference=docker://${IMAGE_REF}@${digest}"
            } >>"${GITHUB_OUTPUT}"
          else
            {
              echo 'available=false'
              echo 'digest=none'
            } >>"${GITHUB_OUTPUT}"
            echo "${BUILD_PROFILE}: no verifiable previous profile build; using a full rechunk" >&2
          fi

      - name: Rechunk bootc image for efficient updates
        if: steps.reuse.outputs.hit != 'true'
        id: rechunk
        env:
          BUILD_PROFILE: ${{ inputs.profile }}
          PREVIOUS_BUILD: ${{ steps.previous_build.outputs.reference }}
        shell: bash
        run: |
          primary_image="${IMAGE_REF}:${BUILD_PROFILE}-candidate"
          remote_ref="${primary_image}"
          output="docker://${remote_ref}"
          rechunk_args=(
            --source "${primary_image}"
            --output "${output}"
            --authfile "${REGISTRY_AUTH_FILE}"
          )
          if [[ -n "${PREVIOUS_BUILD}" ]]; then
            rechunk_args+=(--previous-build "${PREVIOUS_BUILD}")
          fi
          report="$({
            PURPLEFIN_PODMAN="$(command -v podman)" \
              nix shell --accept-flake-config .#ci-rechunk-image \
                -c purplefin-rechunk-image "${rechunk_args[@]}"
          })"
          {
            echo "digest=$(jq -er .digest <<<"${report}")"
            echo "mode=$(jq -er .mode <<<"${report}")"
            echo "previous_build_digest=$(jq -er .previous_build_digest <<<"${report}")"
            echo "seconds=$(jq -er .rechunk_seconds <<<"${report}")"
          } >>"${GITHUB_OUTPUT}"

      - name: Record staged candidate digest
        id: push
        env:
          BUILD_PROFILE: ${{ inputs.profile }}
          RECHUNK_DIGEST: ${{ steps.rechunk.outputs.digest }}
          REUSED_DIGEST: ${{ steps.reuse.outputs.digest }}
        shell: bash
        run: |
          primary_ref="${IMAGE_REF}:${BUILD_PROFILE}-candidate"
          digest="${REUSED_DIGEST:-${RECHUNK_DIGEST}}"
          [[ "${digest}" =~ ^sha256:[0-9a-f]{64}$ ]]

          published_digest="$({
            nix shell --accept-flake-config .#ci-skopeo \
              -c skopeo inspect --retry-times 3 --format '{{.Digest}}' \
                "docker://${primary_ref}"
          })"
          [[ "${published_digest}" == "${digest}" ]]

          echo "digest=${digest}" >>"${GITHUB_OUTPUT}"

      - name: Sign and verify immutable image
        if: steps.reuse.outputs.hit != 'true'
        env:
          COSIGN_IDENTITY: https://github.com/${{ github.repository }}/.github/workflows/build-profile.yml@refs/heads/main
          DIGEST: ${{ steps.push.outputs.digest }}
        shell: bash
        run: >-
          nix shell --accept-flake-config .#ci-image-sign
          -c purplefin-image-sign

      - name: Check for verified build provenance
        id: provenance_check
        env:
          DIGEST: ${{ steps.push.outputs.digest }}
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          if gh attestation verify "oci://${IMAGE_REF}@${DIGEST}" \
            --bundle-from-oci \
            --repo "${GITHUB_REPOSITORY}" \
            --signer-workflow "${GITHUB_REPOSITORY}/.github/workflows/build-profile.yml" \
            --source-digest "${GITHUB_SHA}" \
            --predicate-type https://slsa.dev/provenance/v1 >/dev/null 2>&1; then
            echo 'hit=true' >>"${GITHUB_OUTPUT}"
          else
            echo 'hit=false' >>"${GITHUB_OUTPUT}"
          fi

      - name: Attest build provenance
        if: steps.provenance_check.outputs.hit != 'true'
        id: provenance_first
        continue-on-error: true
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          push-to-registry: true
          subject-digest: ${{ steps.push.outputs.digest }}
          subject-name: ${{ env.IMAGE_REF }}
          subject-version: ${{ inputs.version }}

      - name: Retry build provenance attestation
        if: >-
          steps.provenance_check.outputs.hit != 'true' &&
          steps.provenance_first.outcome == 'failure'
        id: provenance_retry
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          push-to-registry: true
          subject-digest: ${{ steps.push.outputs.digest }}
          subject-name: ${{ env.IMAGE_REF }}
          subject-version: ${{ inputs.version }}

      - name: Verify build provenance
        env:
          DIGEST: ${{ steps.push.outputs.digest }}
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          for _ in {1..6}; do
            if gh attestation verify "oci://${IMAGE_REF}@${DIGEST}" \
              --bundle-from-oci \
              --repo "${GITHUB_REPOSITORY}" \
              --signer-workflow "${GITHUB_REPOSITORY}/.github/workflows/build-profile.yml" \
              --source-digest "${GITHUB_SHA}" \
              --predicate-type https://slsa.dev/provenance/v1 >/dev/null; then
              exit 0
            fi
            sleep 5
          done
          echo 'The published build provenance could not be verified' >&2
          exit 1

      - name: Summarize profile build
        if: always()
        env:
          BUILD_INPUT: ${{ inputs.build-input }}
          BUILD_OUTCOME: ${{ steps.build.outcome }}
          BUILD_PROFILE: ${{ inputs.profile }}
          BUILD_SECONDS: ${{ steps.build.outputs.seconds }}
          CACHE_AVAILABLE: ${{ steps.build.outputs.cache_available }}
          CACHE_REF: ${{ steps.build.outputs.cache_ref }}
          CACHE_WRITE: ${{ steps.build.outputs.cache_write }}
          IMAGE_DIGEST: ${{ steps.push.outputs.digest }}
          PARENT_DIGEST: ${{ steps.inputs.outputs.parent_digest }}
          PROVENANCE_FIRST_OUTCOME: ${{ steps.provenance_first.outcome }}
          PROVENANCE_HIT: ${{ steps.provenance_check.outputs.hit }}
          PROVENANCE_RETRY_OUTCOME: ${{ steps.provenance_retry.outcome }}
          RECHUNK_OUTCOME: ${{ steps.rechunk.outcome }}
          RECHUNK_MODE: ${{ steps.rechunk.outputs.mode }}
          RECHUNK_PREVIOUS_DIGEST: ${{ steps.rechunk.outputs.previous_build_digest }}
          RECHUNK_SECONDS: ${{ steps.rechunk.outputs.seconds }}
          REUSE_DIGEST: ${{ steps.reuse.outputs.digest }}
          REUSE_HIT: ${{ steps.reuse.outputs.hit }}
          REUSE_SECONDS: ${{ steps.reuse.outputs.seconds }}
          UPSTREAM_LOAD_SECONDS: ${{ steps.upstream_load.outputs.seconds }}
        shell: bash
        run: |
          finished_at="$(date +%s)"
          started_at="${BUILD_STARTED_AT:-${finished_at}}"
          [[ "${started_at}" =~ ^[0-9]+$ ]] || started_at="${finished_at}"
          total_seconds=$((finished_at - started_at))

          mode=stage
          image_digest="${IMAGE_DIGEST:-unavailable}"
          attestations="provenance=reused"
          if [[ "${PROVENANCE_HIT}" != true ]]; then
            attestations="provenance=${PROVENANCE_FIRST_OUTCOME:-skipped}/${PROVENANCE_RETRY_OUTCOME:-not-needed}"
          fi

          {
            echo "### Profile ${BUILD_PROFILE}"
            echo
            echo '| Property | Value |'
            echo '| --- | --- |'
            echo "| Mode | \`${mode}\` |"
            echo "| Build input | \`${BUILD_INPUT}\` |"
            echo "| Parent digest | \`${PARENT_DIGEST:-unresolved}\` |"
            echo "| Image digest | \`${image_digest}\` |"
            echo "| Published image reuse | \`${REUSE_HIT:-false}\` (${REUSE_SECONDS:-0}s; \`${REUSE_DIGEST:-none}\`) |"
            echo "| Registry cache | \`${CACHE_AVAILABLE:-unknown}\` available; \`${CACHE_WRITE:-false}\` updated; \`${CACHE_REF:-unresolved}\` |"
            echo "| Upstream load | ${UPSTREAM_LOAD_SECONDS:-0}s |"
            echo "| Container build | \`${BUILD_OUTCOME:-skipped}\` (${BUILD_SECONDS:-0}s) |"
            echo "| Rechunk | \`${RECHUNK_OUTCOME:-skipped}\` (${RECHUNK_SECONDS:-0}s; \`${RECHUNK_MODE:-not-run}\`; previous \`${RECHUNK_PREVIOUS_DIGEST:-none}\`) |"
            echo "| Attestations | \`${attestations}\` |"
            echo "| Total duration | ${total_seconds}s |"
          } >>"${GITHUB_STEP_SUMMARY}"

      - name: Log out of GHCR
        if: always()
        continue-on-error: true
        shell: bash
        run: |
          registry_auth_file="${REGISTRY_AUTH_FILE:-${HOME}/.docker/config.json}"
          podman logout --authfile "${registry_auth_file}" ghcr.io || true
          rm -f "${registry_auth_file}"
